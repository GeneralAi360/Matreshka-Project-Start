# Claude Code adapter

Keep the canonical project guidance in `AGENTS.md`. If the project owner requests a Claude-specific bridge, create a small `CLAUDE.md` that links to the root `AGENTS.md`, project profile and approved workflows rather than creating a divergent policy copy.

Translate a role from `agents/` into a Claude Code subagent only when the environment supports it and the owner approved the relevant tool access.
