# Antigravity adapter

Use the same canonical `AGENTS.md`, project profile and agent contracts. Add only the native project-instruction bridge supported by the installed Antigravity version. The bridge must link to canonical files and must not create parallel workflow rules.

Enable browser, GitHub, server or deployment tools separately; the presence of a role contract is not tool authorization.
